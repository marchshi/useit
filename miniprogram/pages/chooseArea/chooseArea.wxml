<!--miniprogram/pages/chooseArea/chooseArea.wxml-->
<view class="container">
<mp-cells title="片区列表">
  <view wx:for="{{areaList}}" wx:key="unique">
    <mp-cell link hover url="/pages/chooseBlock/chooseBlock?id={{item.id}}" value="{{item.areaName}}"></mp-cell>
  </view>
</mp-cells>
</view>